services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:80"
    restart: unless-stopped

  backend:
    build:
      context: ./backend
    env_file:
      - ${BACKEND_ENV_FILE:-/etc/local-buddy/backend.env}
    ports:
      - "8080:8080"
    restart: unless-stopped
